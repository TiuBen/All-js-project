最后提醒一个非常重要的问题

如果你使用的是 Vite + SPA，即使前端能解析 /duty-record，浏览器 F5 还是可能返回 404，因为请求会发给开发服务器。

需要在 vite.config.js（开发）或 Nginx（生产）配置：

所有未知路径
        │
        ▼
返回 index.html
        │
        ▼
React 再根据 pathname 自己渲染页面

如果你的目标是做一个后台管理系统，我建议把整个 pageRegistry + Zustand Router 改造成一个完整的轻量 Router，比现在更清晰，也方便以后支持参数路由、返回、面包屑和 KeepAlive