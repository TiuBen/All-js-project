const WsInfo = require("../models/index");

module.exports = async (req, res, next) => {
    console.log("查询所有的通知");
//     try {
//         const allWsInfp = await WsInfo.findAll({
//             attributes: ["id", "creator", "content","readed"],
//         });
//         if (allWsInfp !== null) {
          
//         } else {
//             res.status(400).send({ message: "something wrong" });
//         }
//     } catch (error) {
//         res.status(500).send({ message: `checkin getAllByUser 有问题!` });
//     }
};
