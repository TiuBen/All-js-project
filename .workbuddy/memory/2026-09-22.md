
# 2026-09-22

## 18:55 提交成功跳详情页（/checklists/:checkedId）
- Toolbar：新增 handleSubmit = await submit() → record.id 存在则 navigate(`/checklists/${record.id}`,{replace:true})；按钮 onClick 由 submit 改 handleSubmit（replace 顶掉编辑器历史，返回键回航班列表）
- store 新增 action loadRecord(checkedId)：查看页唯一数据入口。getRecord → 模板查表(checklist_category→TEMPLATES，兜底货运过站) → flightOfRecord(记录字段 + manual_fips 反查行归一 origin/destination/flightDate/uuid) → 写 recordId/loadedRecord/recordStatus/checkedAt/flight/header/template → hydrateFromRecord 灌填写层 + setDraftContext(flightId=uuid优先)
- 模块级 helper：datePartOf(只取 YYYY-MM-DD 前缀,避免把 "10:30" 当日期)/findSourceFlight(flight_id 取尾部数字匹配,先查 manualFipsStore 缓存再 manualFipsApi.list,失败返回 null 不影响打开)
- ChecklistViewer 零 props 化：useParams().checkedId + useEffect(store 已是同条则跳过请求)；flight 为空首帧渲染 loading；activeBtn 用 typeColorOf(tplId)；「修改」→ /checklists/flight/${flight.uuid || flight.queryId}（编辑器本身 store 驱动，参数仅为 URL 身份）
- 观察：DB 里 checklist_records.flight_id 历史两种写法 manual-2/manual-3（旧）与 5/6/7（现 submit 传数字 id）→ 后端 syncChecklistUuid 正则 /^manual-(\d+)$/ 对纯数字不匹配，manual_fips.checklist_uuid 不会被回写（未改，待用户定夺：统一成 manual-{id} 会与现存 5/6/7 记录脱钩）
- build EXIT 0

## 19:15 草稿键改「航班 × 类型」复合键 + 切草稿串号根治
- 用户反馈：多航班草稿在本地缓存里分不开，切另一个航班会拿到别人的草稿
- 真根因（两条叠加）：① 编辑器是 store 驱动且**不读 URL**，只换 URL 不重装 → 页面继续显示上一个航班 + 填写层留着上个航班的 items，800ms 后把 items 落到**上一个航班的键**上；② openDraft 在"没找到草稿"时不清理 items，同样残留
- 修法 1 复合键：draftKeyOf(航班键,模板id) = uuid::类型名；IndexedDB **DB_VERSION 2 / keyPath 'key'**（upgrade 删旧 store 重建，旧草稿作废）；draftStore upsertDraft/removeDraft/getDraft 改按 key；新增 removeDraftsByFlight；checklistDraft 新增 removeIdbDraftsByFlight(flightKey)，removeIdbDraft 改收 key
- 修法 2 openDraft(flightKey, templateId?)：传类型精确取，不传则 idbGetByFlight + pickLatest（草稿箱继续编辑用）；**无论有无草稿都整体替换内存**（无 → 空草案）
- 修法 3 store.openFlight(flightKey, templateId?)：编辑器 URL 唯一装载入口。同航班且未指定类型 → 直接返回（航班列表点进来的路径已装好）；同航班指定类型 → switchChecklistType；换航班 → reset + findFlightByKey + setFlight + openDraft + loadTemplate(草稿类型优先) + setDraftContext
- 修法 4 switchChecklistType 语义改为"换一份草稿"：flushDraft() 先存旧类型 → openDraft(键,新类型) → loadTemplate（不再 clearMemory，切类型不再丢另一类型的草稿）
- 修法 5 驱动源收敛：类型下拉 handleSelect 只写 URL（删掉它自己的 URL→switch effect，避免两处驱动重复装载）；草稿箱两处（DraftDropdown/DraftSection）跳转带 ?checkTemplate=类型；删除草稿同时删 IDB 正文（原来只删 localStorage 索引 → 会复活）
- 顺带：DraftSection 里遗留调试行 JSON.stringify(drafts.id) 删掉；草稿卡片/下拉显示 templateId 以区分同航班不同类型；编辑器 flight 为空时渲染 loading 守卫；FipsPage.handleCreateChecklist 简化为 reset + navigate（装载交给 openFlight）
- 验证：vite build EXIT 0 + 临时 SSR 探针 _probe_draft.mjs 10/10（键区分 3 项、切航班清空、同航班换类型清空、无 IDB 降级、hardReset），探针已删

## 19:55 查看页出口改「新建」+ 填写记录页落地
- 需求1 ChecklistViewer 出口：默认「新建检查单」(FilePlus2) = getState 取 flightKey/类型 → reset()（清 recordId，否则提交走 update）→ navigate /checklists/flight/:key?checkTemplate=N（编辑器 openFlight 重装，草稿已随提交清空 → 空表 → 提交走 create，后端按 flight_id upsert）；「修改」降为次要按钮（带原内容回编辑器）；两者 24h 锁定后禁用
- 需求2 填写记录页（仿 FipsPage：ContentLayout+Sidebar+Card 表格）
  - 新建 pages/RecordsPage/RecordsPage.jsx + components/RecordsSidebar.jsx（FlightSearchCard + DateFilterPanel(dayMarkers) + 选中操作卡）
  - 列：创建日期(日期+时刻两行)/航班号/机型/检查单类型(色点 inline style)/检查人/状态/异常(红底数字,0 显示—)/最后更新 + 操作(查看)；列头三态排序；关键词过滤(航班号/机型/类型/检查人/flight_id)；选中行→侧栏查看详情/删除；双击行=查看
  - 日期口径改为**创建日 created_at**：后端 checklistService.listRecords 的 date/from-to 过滤与 ORDER BY 全部 COALESCE(created_at, updated_at)；recordsStore.fetchDayMarkers 同步；新增导出 countAbnormal(record)（items + video_supervision）供表格与徽标同源；fetchRecords 增加 error 态
- 验证：vite build EXIT 0；临时探针 _probe_records.mjs 12/12（异常统计 3 项/创建日分组关键用例 09-21 不被更新日拉走/取 appStore 参数/失败降级）；curl 后端 date=2026-09-22→19,18 · 09-20→17 · 09-12→15（顺序已按 created_at DESC，后端 watch 自动重载）；探针已删
- ⚠️ SSR 渲染验证受阻：react-router-dom v7 的 dist 是 CJS/ESM 双语法文件，Node 原生 import 报 ERR_AMBIGUOUS_MODULE_SYNTAX；vite ssr.noExternal:true 又让 react(CJS) 报 module is not defined —— 故渲染层改用浏览器实测（已装 agent-browser）
- 浏览器实测（系统 Chrome 无头，agent-browser 下 Chromium 超时失败）：/records 渲染 2 行（CSS102 异常1红标 / CSS101 —）、徽标 3 个(09-12 红 / 09-20 绿 / 09-22 红)、无 vite-error-overlay；/checklists/18 查看页右上显示「新建检查单」+「修改」；/checklists/flight/{uuid}?checkTemplate=3 编辑器为空表（主要监控 14 项全待检查、辅助 0、视频 40，填写值全空）——证明「新建」路径装载正确
- 截图归档 .workbuddy/screenshots/2026-09-22-records-page/{records,viewer,editor-new}.png；临时 _shots/_probe_* 已清
- 遗留未动：manual_fips.checklist_uuid 仍只对旧记录 13/15/16 有值（现 submit 传数字 flight_id，后端 syncChecklistUuid 正则 /^manual-(\d+)$/ 不匹配纯数字）

## 21:10 新建/更新/删除 语义分离（"点创建检查单却改了旧记录"根治）
- 用户反馈：已创建过的航班再点"创建检查单"，结果还是更新了过去的检查单 → 要求创建就是创建、更新就是更新；store 里分别写出对应的后端交互函数，不同按钮调不同方法
- 真根因：**后端 POST 按 flight_id upsert**（`checklist_records` 上还有 `idx_records_flight_unique` 唯一索引）—— 前端 reset 后确实调的 POST，但 POST 自己把旧记录改了；前端 submit() 又靠 recordId 隐式分流，两处叠加
- 修后端：① createRecord 删掉 upsert，改成纯 INSERT（单航班→多份检查单）；② schema.sql/schema.js 把唯一索引换成 `idx_records_flight` 普通索引（schema.js 里加 `DROP INDEX IF EXISTS idx_records_flight_unique` 幂等迁移）；③ 新增 `parseFlightRef`（认 fips-N / manual-N / 裸数字=manual_fips）+ `syncChecklistUuid(flightId)` 改为**子查询重算 = 该航班最新一份 id**（无则 NULL），create/update/delete 都调它 —— deleteRecord 里原来手写 UPDATE ... NULL 的那段删了，删掉最新一份会自动指回上一份；④ 控制器/路由注释同步（POST=新建、PUT=更新、DELETE=删除）
- 修前端 store（三个显式动作 + 共用 helper）：factory 内新增 `prepareSubmit()`（航班/模板/24h 锁定）/`buildPayload(s,d,status)`（header 注入模板元信息）/`afterSaved(record,status,s)`（记录元数据 + recordsStore.refresh + 清该航班草稿+resetDraftTracking）；`createRecord(opts)`=POST（成功后写回 recordId）、`updateRecord(opts)`=PUT（无 recordId 直接报错，**绝不隐式降级成新建**）、`deleteRecord(id?)`=DELETE（删的是当前会话这条则退出修改态）、`submit` 降级为一行分流器
- 修前端路由/按钮（URL 决定提交语义，不靠 store 残留）：新增 `openRecordForEdit(recordId, fallbackFlightKey)`（reset→loadRecord 灌数据；记录已被删则退回 openFlight 新建态 + saveError 提示，不再卡 loading）；`openFlight` 的"同航班早退"加 `&& !s.recordId`；编辑器 effect 读 `?record` 分支；Toolbar 提交按钮 = `recordId ? updateRecord() : createRecord()`，文案「保存修改/提交」+ title；InfoDisplay 加「修改 #id / 新建」chip；ChecklistViewer「修改」→ `?record=<checkedId>`（新建仍不带）；RecordsPage 行内 + 侧栏新增「修改」按钮 → `?record=<id>`
- DB 迁移：DROP idx_records_flight_unique + CREATE INDEX idx_records_flight + 按新口径重算全部 manual_fips.checklist_uuid（顺带修好了 6/7 号航班此前不回写的遗留）
- 验证：临时探针 `tmp/_verify-crud.mjs`（node fetch 打 5183 + pg 校验关联）**11/11** —— 同一航班连续两次 POST 得 id=22/23 两条不同记录、记录数 +2 且旧记录未动、PUT 只改目标那条（另一条 inspector 与 category 原值不动）、checklist_uuid = 最新一份、DELETE 一条后关联自动回退到仍在的那份；跑完自动清理并把库还原成 13/15/18/19
- 浏览器实测（系统 Chrome 无头 --dump-dom，用户 dev server 在 5173）：`/checklists/flight/7?record=19` → chip「修改 #19」+ 按钮「保存修改」+ 航班 CSS102 已装载；`/checklists/flight/7` → chip「新建」且无「保存修改」；`?record=99999` → 提示"不存在或已被删除"并退回新建态；`/records` 行内与侧栏都有「修改」
- `vite build` EXIT 0（两次）；临时探针/HTML dump/Chrome profile 已清
- ⚠️ 一个航班现在可以有多份检查单，`manual_fips.checklist_uuid` 语义 = "最新一份"；如果业务上要求"同航班同类型只能一份"，需要另加 (flight_id, checklist_category) 唯一约束并让 POST 冲突时报 409（当前未做，按用户"创建就是创建"的要求放开）

## 21:40 口径确认：一航班允许多份（定稿）+ 摘掉旧语义残留
- 用户确认："确定现在同一航班可以有任意多份检查单（'创建就是创建'的必然结果）" → **不加唯一约束、POST 不返 409**，此口径定稿，不再讨论
- 全链路复核，结论是已彻底贯彻：DB 侧 `checklist_records` 上只剩 pkey(id 唯一) + `idx_records_flight`(普通) + `idx_records_flight_date`(普通)，无 flight_id 唯一索引；`checklistService.createRecord` 是纯 INSERT 无 upsert；`freshAirCargoService` 的 `ON CONFLICT (manual_fips_id)` 与检查单无关（别误判）；前端 FipsPage「创建检查表」无任何"已有检查单→禁用"gate（只 reset + navigate 不带 ?record）
- 清理一处旧语义残留：删掉 `checklistService.findRecordByFlightId`（全仓无调用方，仅剩定义；它是一航班一检查单时代给 upsert 用的"取该航班唯一记录"助手，留着容易被顺手写回 upsert）；`node --check` 通过，nodemon 自动重载
- 复核探针 `tmp/_verify-multi.cjs`（临时，已删）打 5183 **7/7**：同一航班两次 POST → #24/#25 两条独立记录（旧 #19 未动，总数 +2）、PUT 只改目标条（另一条 inspector/分类原值不变）、DELETE 一条后另一条仍在且 `checklist_uuid` 指向剩下的最新一份（25）、跑完自动清理，库还原为 #19 单份
- ⚠️ 由此产生的一个 UX 空档（未做，待用户定）：`/records` 按创建日分日期，一个航班的历史多份检查单会散落在不同日期下；`/fips` 航班行只有「创建检查表」入口，没有"看这个航班已有几份/都是哪些"。可选补法：航班行加「查看检查单 (N)」（需 GET /records?flightId=X 计一次数），或在详情页顶部列出该航班同族记录

