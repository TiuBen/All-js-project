# 2026-09-20 工作日志

### RecordsPage 改名 FipsPage
- 目录 `src/pages/RecordsPage` → `FipsPage`：node renameSync 遭 EPERM、PowerShell Rename-Item 静默失败（目录可能被 watcher 占用）→ 最终 fs 逐文件复制 + 逐一校验存在 + `rmSync` 删源目录成功
- 组件名 `RecordsPage` → `FipsPage`（index.jsx + Sidebar 注释）；App.jsx import 与两条路由（`/records`、`/checklists`）同步；**路由路径未改**
- vite build EXIT 0

### ChecklistEditor/ChecklistViewer 入口文件具名化
index.jsx → ChecklistEditor.jsx / ChecklistViewer.jsx（复制+内容校验+删源）；唯一引用方 ChecklistPage/index.jsx 的 import 改为 ./ChecklistEditor/ChecklistEditor 与 ./ChecklistViewer/ChecklistViewer；两目录内无其它 index 引用。vite build EXIT 0。

### 草稿与后端彻底解耦（/api/manual-fips ERR_CONNECTION_REFUSED 排查）
- 报错根因：首页 FlightSchedulePage 挂载 fetchFlights → /api/manual-fips，后端 5183 没启动 → vite proxy 拒连。**与草稿无关**（草稿在 localStorage `flight_dispatch_drafts` + IndexedDB `flight_dispatch`）
- 查库（pg 直连，密码在 V5-dispatch/.env：postgres/admin@127.0.0.1:5432/flight_dispatch）：**后端无独立草稿表**，只有 checklist_records 里 1 条 status='draft'（id=16 DDD123 客运始发）→ 已删除；现存 2 条 submitted
- 真正 bug：`checklistStore.submit` 默认 `status='draft'` → 工具栏"提交"落库即草稿态（24h 锁定/徽章全失效）→ 默认改 `'submitted'`，注释声明"后端不再有草稿态数据"
- ChecklistPage 加载航班失败 → **草稿箱快照兜底**（draftStore.getDraft 条目自带 flightNo/aircraftType/templateId → setFlight + openDraft + loadTemplate + setDraftContext），后端没开也能继续填草稿
- FlightSchedulePage 挂载 fetchFlights 加 .catch(()=>{}) 兜未处理 rejection；浏览器网络红字在后端未启动时仍会出现（无法避免）
- vite build EXIT 0


## 2026-09-20 00:55 MainPageLayout 与 App.jsx 结合
- 用户新建 src/Layout/MainPageLayout.jsx 实为 0 字节空文件（编辑器未保存）；已按 PageLayout 原职责写入（顶部导航+children）
- App.jsx 顶层改用 MainPageLayout；旧 components/PageLayout.jsx 已删
- 修复 ContentLayout 迁到 src/Layout 后三处断裂 import（FreshGuaranteePage/FlightSchedulePage/FipsPage）
- 修复 FlowChart 引用断裂：DraggableThumb/FlowStatusView → pages/FlightSchedulePage/flowchart/FlowChart；其自身 lib/utils 路径少一层
- App.jsx 中 ChecklistSelectPage/ChecklistRecordPage 引用与路由注释（页面未建，留 TODO）
- vite build EXIT 0；发现 pages/ChecklistDrafts 死代码（import 全断且无人引用）
- MEMORY.md 超限已精简重写

## 2026-09-20 01:09 ChecklistDrafts 报错解决
- pages/ChecklistPage/ChecklistDrafts 实为 ChecklistSelectPage（组件名即此）→ 搬迁为 pages/ChecklistSelectPage/（cpSync 校验后删旧目录）
- import 路径修正：新位置比旧位置浅一层（../../store 即 src/store）；第一次多加一层 build 失败，回退后 EXIT 0
- App.jsx 用户已自行改：ChecklistRecordPage 别名指向 RecordsPage（/records 路由），ChecklistSelectPage import 由我改指新位置，/checklists 路由恢复
- ⚠️ 教训：搬迁目录后相对层级变化要按「新位置深度」重算，不是旧路径 +1

## 2026-09-20 01:13 ChecklistSelectPage 重命名为 Drafts
- pages/ChecklistSelectPage → pages/Drafts（同深度,相对 import 不变）,组件名改 Drafts
- App.jsx：import Drafts from "./pages/Drafts",/checklists 路由 element=<Drafts/>
- 用户同期把 RecordsPage/index.jsx 改名 RecordsPage/RecordsPage.jsx → App.jsx import 补全路径
- build EXIT 0

## 2026-09-20 01:15 Drafts 移入 ChecklistPage
- pages/Drafts → pages/ChecklistPage/Drafts，层级 +1：index.jsx ../../→../../../，DraftSection.jsx ../../../→../../../../（store/ui）
- App.jsx import 改 ./pages/ChecklistPage/Drafts；build EXIT 0

## 2026-09-20 01:32 Outlet 化路由重构
- 用户重构 pages：FlightSchedulePage 并入 FipsPage.jsx（flowchart/components 随迁）；FreshGuaranteePage→SpecialPage；RecordsPage.jsx 置为占位 stub；ChecklistPage 入口 index.jsx 被删（git HEAD 版本过旧不可用,VS Code 历史 8-30 也旧,未恢复）
- MainPageLayout 改 Outlet 版（不收 children,tabs: /fips /checklist /special /records）
- App.jsx 重写为嵌套路由：布局路由包全部子路由；/→/fips；/checklists/:checkedId 直挂 ChecklistViewer
- 修 DraggableThumb/FlowStatusView 的 FlowChart 引用 → pages/FipsPage/flowchart/FlowChart；全扫 import 无悬空
- build EXIT 0
- ⚠️ 遗留：/checklists/:checkedId 的编辑态（?mode=edit）无分发入口,ChecklistViewer 收 props 裸挂可能有运行时风险

## 2026-09-20 01:52 创建检查表经 store 传航班
- FipsPage.jsx 新增 handleCreateChecklist：reset→setFlight(蛇形补驼峰 flightNo/aircraftType)→openDraft(IDB 恢复草案)→loadTemplate(FALLBACK 货运过站)→setDraftContext→navigate /checklists/flight/:id
- 修复原按钮 URL 笔误 /checklists/flightID=xx（路由不匹配落兜底）；不写 checkTemplate 参数——下拉 URL 同步 effect 会 switchChecklistType→clearDraftMemory 冲掉恢复的草稿
- build EXIT 0

## 2026-09-20 02:00 航班身份改用数据库 uuid
- DB：manual_fips 加列 uuid varchar(64) NOT NULL DEFAULT gen_random_uuid()::text + 唯一索引 manual_fips_uuid_key；5 条旧数据已回填；schema.sql 同步（CREATE TABLE + INDEX）
- 坑：psql -c 多语句单事务，一句报错全体回滚（第一次 ALTER 白做）；COALESCE(varchar, uuid) 类型不匹配需 ::text
- 后端无需改：listManualFips SELECT m.* / createManualFips RETURNING * 自动带出 uuid
- 前端 handleCreateChecklist 身份改 uuid：openDraft(f.uuid)/setContext({flightId:uuid})/URL /checklists/flight/:uuid；flightNo 仅展示
- checklistStore.submit 草稿清理 flight.id→flight.uuid；API payload 保持数字 id
- 旧 IndexedDB 草稿键是数字 id → 已作废；build EXIT 0

## 2026-09-20 02:22 DraftDropdown 弹层视口适配
- 原弹层 absolute left-0 top-full w-80：按钮在工具栏最右侧 → 弹层撑出屏幕右缘；宽窄窗口均溢出
- 改为打开时 getBoundingClientRect 实测定位 + fixed 定位（逃逸父级 overflow/transform 裁剪）：水平默认按钮右缘对齐再钳进视口（min(320,vw-16) 宽）；垂直优先向下，下方 <360px 且上方更宽裕则向上翻
- 面板 flex-col + 列表 flex-1 min-h-0，maxHeight 按可用空间给，列表不再写死 max-h-72
- 外点关闭需同时排除按钮容器与 panelRef（fixed 后弹层不在 ref 内）
- build EXIT 0

## 2026-09-20 02:33 通用 Dropdown 组件（Radix Popover 封装）
- 用户新建 components/ui/Dropdown.jsx：基于已装的 @radix-ui/react-popover 封装（export Dropdown/DropdownTrigger/DropdownContent/DropdownClose；默认 align=end sideOffset=8 collisionPadding=8，Portal 渲染）——定位/翻转/外点/Esc 全交 Radix
- DraftDropdown 重写为受控 Dropdown：删掉手写 place()/外点监听/resize 监（约 45 行）；受控 open 以便展开时才 getState().drafts；max-h 用 min(360px,calc(100vh-24px))
- 选择 Popover 而非 DropdownMenu 的理由：场景是富内容面板非菜单项；Radix 定位能力一致
- ⚠️ 编辑并发：写文件前用户又改了 DraftDropdown（title 文案/删底栏），按其最新版重写
- build EXIT 0
