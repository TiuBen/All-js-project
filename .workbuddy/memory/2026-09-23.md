# 2026-09-23 工作日志

## 修复 BaseCheckInput 截图「没有任何预览」+ 辅助项接入同一套输入组件

### ① 根因：StrictMode 双调用把 objectURL 提前撤销（不是 CSS、不是粘贴逻辑）
- 真机 CDP 复现（dev 5174，`main.jsx` 开着 StrictMode）：粘贴后的 `<img>` **`naturalWidth = 0`**，`fetch(blob:…)` 直接 `Failed to fetch`；而"再选一张文件替换"却正常（`320×200` 能解码）
- 差别在于是否是 **CheckImage 的首次挂载**：StrictMode dev 下 effect 是「挂载 → 立刻卸载 → 再挂载」，原代码在卸载清理里同步 `URL.revokeObjectURL(useMemo 刚建的那个 url)` → URL 在真挂载前就作废 → 图永远出不来（换成"替换"时组件已挂着，清理的是旧 URL，所以看起来"换一张就好"）
- 上一轮的截图之所以看着正常，是"图片加载任务"与"passive effect 清理"的竞态，**偶然**跑赢了 —— 属于 flaky，不是真的好了

### ② 修法（`src/components/CheckImage.jsx`）
- `revokeLater(url)`：把 revoke 放进 `setTimeout(…, 0)`；`keepAlive(url)` 在重挂载时 clearTimeout 取消。StrictMode 的假卸载与重挂载在同一任务内完成，定时器来不及跑 → 真卸载才释放，假卸载被取消
- 验证：粘贴 / 文件选择 / 刷新后从 IndexedDB 恢复，三条路径 `naturalWidth` 均 > 0

### ③ 辅助项接入（`AuxiliaryCheckItem.jsx`）
- 备注 textarea（`{...register("note")}`）→ `BaseCheckInput`（`note` + `image` 受控）；时间输入保留 `register("time", { also:{ auto:false } })`
- 布局：实际时间单独一行，BaseCheckInput 全宽一行（截图工具条在 360px 侧栏里需要宽度）
- 配套（否则截图会静默丢）：
  - `checklistDraft.boxSnapshot()` 的 image 剥离**同时作用于 items 与 videoItems**（原先只处理 videoItems → 辅助项的 Blob 进 localStorage 会变成 `{}`）
  - `checklistStore.uploadDraftImages()` 改为返回 `{ items, videoItems }`，两份都扫、都把内存里的 blob 换成 url；两处调用点改为 `buildPayload(s,{ ...d, items, videoItems })`
  - 查看页 `AuxiliaryList` 加 `CheckImage` 展示辅助项截图；顺手修掉 `{aItem.note}是` 的笔误
  - 文档口径同步：`useRegister.js`（说明 note 不再走 register）、`checkImage.js`（引用方列表）、`CheckImage.jsx`

### ④ 验证
- CDP 端到端 **11/11**：视频项粘贴缩略图可解码 → 刷新后从 IndexedDB 恢复仍可解码 → 辅助项文件选择出缩略图 → 截图以 Blob 落进 `items['aux-…']`（含备注）→ 提交后 `items` 与 `video_supervision` 都带 `/api/uploads/*.png` 且文件可取回（字节数一致）→ 查看页展开节点后辅助项与视频面板都能渲染服务端图
- 探针创建的记录（#33）与上传的两张图**已删除**，`data/uploads/` 恢复为空；`vite build` EXIT 0

### ⑤ 归档
- 效果截图 + 说明：`.workbuddy/screenshots/2026-09-23-aux-image-input/`

---

## 查看页重构：模板目录改名 + 扁平视图数据 + Tree 改三个 List

### ① 目录改名 `Template/TemplateJson` → `Template/EditorTemplateJson`
- 8 个数据文件整体 `renameSync`；引用点全量更新：checklistStore（5 条 import + 1 处注释）、`Template/*.jsx` ×5（各 2 条 import）、ChecklistEditor/VideoPanel/ShunhangFlight/checklistDraft/shunhangFlight.js 注释、`scripts/gen-checklist-static.cjs`（OUT 路径 + 说明）、`AI/continue.md` 目录树
- ⚠️ **踩到"编辑器旧缓冲回写"**：头几次 Edit 报成功、grep 回读却还是旧串（checklistStore 5 条 import 与脚本两处被还原）。改用"一次性 `.cjs` 批量替换 + `includes()` 计数校验 + 写后回读断言"，跑完再 grep 复核才干净

### ② 新增 `Template/ViewTemplateJson`（查看页扁平一维视图数据）
- 新生成脚本 `scripts/gen-view-static.cjs`：从 **EditorTemplateJson** 提取，产出 **5 类型 × 3 列表 = 15 个文件**（`xxxFlight\{Main,Auxiliary,Video\}ViewJson.js`，导出 `XXX_FLIGHT_MAIN_VIEW_JSON` 等，导出名**手写不自动转换**——沿用 SHUNHANGVIDEOFOCUS 那次翻车教训）
- 拍平口径：Main = `schema[]` 一维（`key=main-<uuid>`、`order`）；Auxiliary = 各节点 `auxiliaries[]` 汇总（带 `parentUuid/parentName/parentOrder`，键 `aux-<uuid>`）；Video = `groups[].items[]` 一维（带 `groupUuid/groupName`，键 = uuid）。**key 与填写端写进 `items` / `video_supervision` 的键完全一致** → List 直接 `items[row.key]`
- 生成基线：主 0/9/14/13/17、辅 0/22/24/0/0、视频 42/40/40/38/38

### ③ 新增 `src/utils/ViewColor.js`（查看页配色唯一来源）
- 异常 `#dc2626` 红；**不适用 / 待检查 / 正常 暂时统一灰 `#64748b`**；`viewStatusOf` 归一化（空/未填→pending）、`VIEW_STATUS_ORDER`（异常排最前）、`countViewStatus(items)`、`VIEW_LIST`（三列表标题+主色，现统一中性灰）。纯数据+纯函数，**不 import store**（否则与 store→utils 成环）

### ④ `ChecklistViewer/Components`：Tree 改写为三个 List
- 新增 `MainCheckList.jsx` / `AuxiliaryCheckList.jsx` / `VideoCheckList.jsx`（**零 props**：自订阅 `template.id` + `loadedRecord`，各自带 category→视图数据映射表，未知类型回退 `FALLBACK_TEMPLATE_ID`）+ 公共外壳 `ViewListShell.jsx`（统计条 + 列表容器，**不做 sticky/fixed 吸附**）+ `statusView.jsx` 改为从 ViewColor 取色（图标形状区分状态，不只靠颜色）
- 删除 `ChecklistTreeView.jsx` / `AuxiliaryList.jsx` / `VideoCheckView.jsx`；视频分组改为"一维数据 + 上一项 groupUuid 变化即插分组标题"，视觉仍分组、结构不再嵌套
- `ChecklistViewer.jsx` 主体改为三列并排（`flex-col xl:flex-row`，窄窗自动堆叠），每列独立滚动、互不联动

### ⑤ 验证
- `vite build` **EXIT 0**（3125 modules）
- 临时 SSR 探针 **63/63 全绿**：15 份视图数据与 EditorTemplateJson 逐一等长/键前缀正确/辅助 parentUuid 都能在主监控里找到 → ViewColor 染色口径 → 三个 List **真实渲染**（统计条计数、红色 inline style、备注与截图、`T→空格`、归属标签「↳ N. 主节点名」、4 个视频分组标题、**无 sticky/backdrop-blur**）→ 顺航空态 → 未知类型回退
- 打通了"渲染订阅 zustand 的组件"的探针手法（桩 store + 原生 react-dom/server + createElement；两个 React 实例坑的破法已写进 MEMORY）
- 归档：`.workbuddy/screenshots/2026-09-23-viewer-three-lists/preview.html`（真实渲染产物 + 内联编译后 Tailwind CSS，可直接打开看三列效果）
- 顺手清理了仓库里遗留的 `_probe26.mjs`（引用已删除的 TemplateJson/index.js，早已失效）
